# Test dependencies (for CI/CD and local testing)
# Install: pip install -r requirements-test.txt

# Include production deps
-r requirements.txt

# ===== Testing =====
pytest>=8.0.0

# CRITICAL: httpx and starlette versions MUST be compatible with fastapi==0.110.0
# fastapi 0.110.0 depends on starlette==0.36.3
# starlette 0.36.3 TestClient requires httpx<0.28.0 (httpx 0.28+ removed 'app' kwarg)
httpx==0.27.0
starlette==0.36.3
